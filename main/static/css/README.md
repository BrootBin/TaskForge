# TaskForge CSS Architecture

## 📁 Структура каталогов

```
css/
├── README.md              # Документация архитектуры CSS
├── main.css              # Главный файл (импортирует все остальные)
├── base/                 # Базовые стили системы
│   ├── reset.css             # Сброс стилей браузера
│   ├── typography.css        # Шрифты и типографика
│   ├── colors.css            # CSS переменные с цветами
│   └── global.css            # Глобальные стили и утилиты
├── components/           # Стили компонентов интерфейса
│   ├── navbar.css            # Навигационная панель
│   ├── buttons.css           # Система кнопок
│   ├── cards.css             # Карточки (привычки, цели)
│   ├── calendar.css          # Календарь и его состояния
│   ├── progress.css          # Элементы прогресса (круги, полосы)
│   ├── modals.css            # Модальные окна
│   ├── forms.css             # Формы и элементы ввода
│   ├── notifications.css     # Уведомления и алерты
│   ├── tables.css            # Таблицы и списки
│   └── animations.css        # Анимации и переходы
├── layout/               # Макеты и структура страниц
│   ├── dashboard.css         # Главная страница дашборда
│   ├── stats-dashboard.css   # Блок статистики
│   ├── sidebar.css           # Боковая панель
│   ├── header.css            # Шапка сайта
│   └── footer.css            # Подвал сайта
└── pages/                # Специфичные для страниц стили
    ├── goals.css             # Страница целей
    ├── habits.css            # Страница привычек
    └── statistics.css        # Страница статистики
```

## 🎨 Дизайн-система

### Цветовая палитра (colors.css)
```css
:root {
  /* Основная палитра */
  --primary-color: #D4AF37;      /* Золотой */
  --secondary-color: #2C3E50;    /* Темно-синий */
  --accent-color: #E67E22;       /* Оранжевый */
  
  /* Семантические цвета */
  --success-color: #27AE60;      /* Зеленый - выполнено */
  --warning-color: #F39C12;      /* Желтый - частично */
  --error-color: #E74C3C;        /* Красный - ошибка */
  --info-color: #3498DB;         /* Синий - информация */
  
  /* Нейтральные цвета */
  --text-primary: #2C3E50;       /* Основной текст */
  --text-secondary: #7F8C8D;     /* Вторичный текст */
  --text-tertiary: #BDC3C7;      /* Третичный текст */
  --bg-primary: #FFFFFF;         /* Основной фон */
  --bg-secondary: #F8F9FA;       /* Вторичный фон */
  --border-color: #E9ECEF;       /* Границы */
}
```

### Типографика (typography.css)
```css
/* Иерархия заголовков */
h1 { font-size: 2.5rem; font-weight: 700; } /* 40px */
h2 { font-size: 2rem; font-weight: 600; }   /* 32px */
h3 { font-size: 1.5rem; font-weight: 600; } /* 24px */
h4 { font-size: 1.25rem; font-weight: 500; }/* 20px */

/* Основной текст */
body { font-size: 1rem; line-height: 1.6; } /* 16px */
.text-small { font-size: 0.875rem; }        /* 14px */
.text-large { font-size: 1.125rem; }        /* 18px */
```

## 🧩 Компонентный подход

### Кнопки (buttons.css)
```css
/* Базовые стили кнопок */
.btn-primary    /* Основная кнопка */
.btn-secondary  /* Вторичная кнопка */
.btn-success    /* Кнопка успеха */
.btn-outline    /* Контурная кнопка */
.btn-ghost      /* Прозрачная кнопка */

/* Размеры */
.btn-sm         /* Маленькая кнопка */
.btn-md         /* Средняя кнопка (по умолчанию) */
.btn-lg         /* Большая кнопка */
```

### Прогресс-элементы (progress.css)
```css
/* Круговые прогресс-индикаторы */
.circle-progress           /* Базовый круговой прогресс */
.progress-ring             /* SVG кольцо прогресса */
.progress-ring__progress   /* Анимированная часть кольца */

/* Линейные прогресс-бары */
.progress-bar              /* Горизонтальная полоса */
.progress-vertical         /* Вертикальная полоса */

/* Состояния прогресса */
.progress--completed       /* 100% выполнен */
.progress--partial         /* Частично выполнен */
.progress--empty          /* Не начат */
```

### Календарь (calendar.css)
```css
/* Основная структура */
.calendar-container        /* Контейнер календаря */
.calendar-header          /* Заголовок с навигацией */
.calendar-grid            /* Сетка дней */
.day                      /* Отдельный день */

/* Состояния дней */
.day.today                /* Сегодняшний день */
.day.habits-completed     /* Все привычки выполнены */
.day.habits-partial       /* Частично выполнены */
.day.empty                /* Пустой день (другой месяц) */
```

## ⚡ Система анимаций

### Переходы (animations.css)
```css
/* Базовые переходы */
.transition-fast    /* 150ms */
.transition-normal  /* 300ms */
.transition-slow    /* 500ms */

/* Специфичные анимации */
.fade-in           /* Плавное появление */
.slide-in          /* Появление сбоку */
.scale-in          /* Масштабирование */
.progress-animate  /* Анимация прогресса */

/* Hover эффекты */
.hover-lift        /* Поднятие при наведении */
.hover-scale       /* Масштабирование при наведении */
.hover-brightness  /* Изменение яркости */
```

### Индикаторы загрузки
```css
/* Состояния загрузки */
.loading          /* Полупрозрачное состояние */
.loading-spinner  /* Вращающийся спиннер */
.loading-dots     /* Анимированные точки */
.skeleton         /* Skeleton loader */
```

## 📱 Адаптивность

### Breakpoints
```css
/* Мобильные устройства */
@media (max-width: 576px) { /* xs */ }

/* Планшеты */
@media (min-width: 577px) and (max-width: 768px) { /* sm */ }

/* Десктопы */
@media (min-width: 769px) and (max-width: 992px) { /* md */ }

/* Большие экраны */
@media (min-width: 993px) { /* lg */ }
```

### Адаптивные утилиты
```css
.hide-mobile      /* Скрыть на мобильных */
.hide-desktop     /* Скрыть на десктопе */
.mobile-only      /* Показать только на мобильных */
.desktop-only     /* Показать только на десктопе */
```

## 🔧 Утилиты и хелперы

### Spacing (global.css)
```css
/* Margin */
.m-0, .m-1, .m-2, .m-3, .m-4, .m-5    /* Все стороны */
.mt-*, .mr-*, .mb-*, .ml-*             /* Отдельные стороны */
.mx-*, .my-*                           /* Горизонтально/вертикально */

/* Padding */
.p-0, .p-1, .p-2, .p-3, .p-4, .p-5
.pt-*, .pr-*, .pb-*, .pl-*
.px-*, .py-*
```

### Display и Flexbox
```css
.d-none, .d-block, .d-inline, .d-flex
.flex-column, .flex-row
.justify-center, .justify-between, .justify-around
.align-center, .align-start, .align-end
.flex-wrap, .flex-nowrap
```

## 🏗️ Принципы архитектуры

### 1. BEM-подобная методология
- `.component` - блок
- `.component__element` - элемент
- `.component--modifier` - модификатор

### 2. Каскадность
```
1. base/ - базовые стили
2. layout/ - структура страниц
3. components/ - компоненты
4. pages/ - специфичные стили
```

### 3. Переменные и кастомизация
- Все значения через CSS переменные
- Легкая смена темы
- Консистентность дизайна

## 📖 Использование

### Подключение
```html
<!-- Только один файл -->
<link rel="stylesheet" href="{% static 'css/main.css' %}" />
```

### Разработка нового компонента
1. Создать файл в `components/`
2. Добавить импорт в `main.css`
3. Использовать переменные из `colors.css`
4. Следовать BEM-подобной структуре

### Кастомизация темы
```css
/* Переопределение в :root */
:root {
  --primary-color: #your-color;
  --bg-primary: #your-bg;
}
```

---

**Версия дизайн-системы:** v2.0 (ноябрь 2025)  
**Последнее обновление:** 4 ноября 2025 г.