# Структура JavaScript в TaskForge

## Обзор
В проекте TaskForge JavaScript-код организован по модульному принципу, что обеспечивает лучшую поддерживаемость, тестируемость и переиспользуемость кода.

## Структура директорий

```
# TaskForge JavaScript Architecture

## 📁 Структура каталогов

```
js/
├── README.md              # Документация архитектуры JS
├── main.js               # Основной файл инициализации
├── debug.js              # Отладочные утилиты
├── components/           # Модульные компоненты
│   ├── calendar.js           # Базовый календарь
│   ├── calendar-habits.js    # Интеграция календаря с привычками
│   ├── habit-checkbox.js     # Обработчики чекбоксов привычек
│   ├── index-subgoals.js     # Обработка подцелей на главной
│   ├── modal.js              # Базовые модальные окна
│   ├── notification.js       # Система уведомлений
│   ├── progress-text.js      # Текстовые индикаторы прогресса
│   ├── stats-dashboard.js    # Дашборд статистики и прогресс-круги
│   ├── subgoal.js           # Обработка подцелей на странице целей
│   ├── support-modal.js     # Модальное окно поддержки
│   └── modals/               # Специализированные модали
│       ├── auth-modal.js         # Авторизация и настройки
│       ├── base-modal.js         # Базовая функциональность модалей
│       ├── create-modal.js       # Создание новых элементов
│       └── 2fa-modal.js         # Двухфакторная аутентификация
├── pages/                # Специфичные для страниц скрипты
└── utils/                # Вспомогательные утилиты
```

## 🚀 Архитектура и принципы

### Модульная структура
- **Компонентный подход**: каждый файл отвечает за конкретную функциональность
- **Слабая связанность**: компоненты могут работать независимо
- **Переиспользование**: общая функциональность выносится в утилиты

### Система инициализации
```javascript
// main.js - главный координатор
document.addEventListener('DOMContentLoaded', function () {
    // Предварительная загрузка критических данных
    if (typeof preloadHabitsData === 'function') preloadHabitsData();
    
    // Инициализация базовых компонентов
    initGlobalUtils();
    initBaseModalHandlers();
    initAuthModals();
    // ... остальные компоненты
});
```

## 📊 Ключевые компоненты

### 📈 Stats Dashboard (`stats-dashboard.js`)
**Назначение:** Управление прогресс-кругами и статистикой
```javascript
// Оптимизированное обновление с кэшированием
updateProgressCircles() → {
    updateDailyProgressCircle()    // 67% текущий прогресс дня
    updateMonthlyProgressCircle()  // 25% месячный прогресс
}
```
**Особенности:**
- Система кэширования API данных
- Индикаторы загрузки (opacity transitions)
- Предотвращение дублирования запросов
- Debouncing для производительности

### 📅 Calendar Habits (`calendar-habits.js`)
**Назначение:** Интеграция календаря с системой привычек
```javascript
// Предварительная загрузка для ускорения
preloadHabitsData() → loadHabitsCompletionHistory()

// Обновление календарных отметок
updateCalendarMarks() → {
    ✅ habits-completed    // Все привычки дня выполнены
    ⚡ habits-partial      // Частично выполнены
}
```
**Особенности:**
- Promise-based кэширование данных
- Система day_was_complete для исторических данных
- Автоматическое обновление календарных отметок

### 🎯 Index Subgoals (`index-subgoals.js`)
**Назначение:** Управление подцелями на главной странице
```javascript
// Инициализация состояния подцелей
initIndexSubgoalHandlers() → {
    applySubgoalCompletionStyle()  // Визуальные стили
    updateGoalProgressLocal()      // Локальный расчет прогресса
    addSubgoalClickHandler()       // Обработчики событий
}
```

### 🔔 Modals System (`modals/`)
**Назначение:** Модульная система модальных окон
- `base-modal.js` - базовая функциональность
- `auth-modal.js` - авторизация, настройки Telegram, 2FA
- `create-modal.js` - создание привычек и целей
- `2fa-modal.js` - двухфакторная аутентификация

## ⚡ Система производительности

### Кэширование данных
```javascript
// Переменные кэша в calendar-habits.js
let habitsHistoryLoaded = false;
let habitsHistoryPromise = null;
let habitsCompletionData = {};

// Умная проверка готовности в stats-dashboard.js
function isHabitsDataReady() {
    return calendarDataReady && 
           typeof habitsCompletionData !== 'undefined' && 
           Object.keys(habitsCompletionData).length > 0;
}
```

### Оптимизация загрузки
- **Предварительная загрузка**: API запросы начинаются до инициализации UI
- **Debouncing**: предотвращение частых обновлений (300ms timeout)
- **Мгновенный fallback**: кэшированные данные отображаются немедленно

### Индикаторы загрузки
```javascript
// Плавные переходы с opacity
element.style.opacity = '0.6';  // Показать загрузку
element.style.transition = 'opacity 0.3s ease';
// После загрузки
element.style.opacity = '1';    // Убрать индикатор
```

## 🛠️ Система событий

### Глобальные события
- `DOMContentLoaded` - основная инициализация
- `habit-completed` - завершение привычки
- `subgoal-updated` - обновление подцели
- `progress-updated` - обновление прогресса

### Локальные обработчики
- Checkbox change events (привычки)
- Click handlers (подцели)
- Modal open/close events
- Form submissions

## 🧪 Отладка и разработка

### Debug утилиты (`debug.js`)
```javascript
// Доступные команды в консоли:
testAuthModal()      // Тест модального окна авторизации
testNotifications()  // Тест системы уведомлений
testProfileBtn()     // Тест кнопки профиля
debugModals()        // Информация о состоянии модалей
```

### Логирование
- Минимальный вывод в production
- Сохранены только критические ошибки и предупреждения
- Отладочная информация доступна через debug.js

## 📱 Совместимость

### Поддерживаемые браузеры
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

### Зависимости
- Vanilla JavaScript (ES6+)
- Chart.js (для графиков)
- Fetch API
- CSS Custom Properties

## 🔧 Разработка

### Добавление нового компонента
1. Создать файл в `components/`
2. Добавить инициализацию в `main.js`
3. Экспортировать основные функции
4. Обновить данный README

### Соглашения по коду
- Функции: `camelCase`
- Константы: `UPPER_SNAKE_CASE`
- Файлы: `kebab-case.js`
- Комментарии: JSDoc стиль для публичных функций

---

**Версия архитектуры:** v2.0 (ноябрь 2025)  
**Последнее обновление:** 4 ноября 2025 г.
```

## Принципы организации

1. **Основные скрипты**:
   - Каждый скрипт отвечает за конкретную функциональную область
   - Все глобальные функции должны быть инкапсулированы в IIFE
   - События инициализируются через DOMContentLoaded

2. **Компоненты**:
   - Отдельные модули для конкретных элементов интерфейса
   - Изолированная логика работы компонентов
   - Минимальная связанность с другими компонентами

3. **Утилиты**:
   - Вспомогательные функции
   - Переиспользуемые в разных частях приложения
   - Не имеют прямой зависимости от компонентов

## Использование

Для использования JS в проекте импортируйте в HTML нужные скрипты. Базовый набор скриптов подключен в `base.html`:

```html
<script src="{% static 'js/main.js' %}"></script>
<script src="{% static 'js/dashboard.js' %}"></script>
<script src="{% static 'js/calendar.js' %}"></script>
<script src="{% static 'js/templates.js' %}"></script>
<script src="{% static 'js/modal.js' %}"></script>
<script src="{% static 'js/stats-dashboard.js' %}"></script>
<script src="{% static 'js/components/progress-text.js' %}"></script>
```

## Правила при разработке

1. **Новые компоненты**:
   - Создавайте отдельный файл в соответствующей директории
   - Добавляйте импорт в нужные шаблоны
   - Используйте IIFE для инкапсуляции кода

2. **Именование**:
   - Используйте camelCase для имен функций и переменных
   - Используйте говорящие имена функций, отражающие их назначение

3. **События**:
   - Используйте делегирование событий где возможно
   - Отделяйте логику от представления

## Преимущества

- Улучшенная поддерживаемость кода
- Возможность переиспользовать компоненты
- Лучшее разделение ответственности
- Упрощение отладки и тестирования