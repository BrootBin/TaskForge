# TaskForge

Сучасний вебзастосунок для керування особистими звичками та цілями з інтеграцією Telegram-бота, real-time сповіщеннями через WebSocket та розширеною аналітикою.

---

## ✨ Основні можливості

### 🎯 Керування цілями та звичками

- **Трекінг звичок** — щоденне відстеження з календарем і лічильником серій (streak)
- **Керування цілями** — створення цілей із підцілями та відстеженням прогресу
- **Шаблони** — готові шаблони цілей і звичок для швидкого старту
- **Статистика** — детальна аналітика виконання з графіками

### 🔔 Система сповіщень

- **Real-time сповіщення** — через WebSocket для миттєвого відгуку
- **Time-based polling** — розумна перевірка сповіщень в активні години (21:00–00:01)
- **Персоналізовані повідомлення** — сповіщення з іменем користувача
- **Візуальні індикатори** — зелені кружки для непрочитаних сповіщень

### 🤖 Інтеграція з Telegram

- **2FA-аутентифікація** — двофакторна авторизація через Telegram
- **Сповіщення** — налаштовувані push-сповіщення в Telegram
- **Керування звичками** — відмітка виконання безпосередньо з бота
- **Статистика** — отримання звітів про прогрес

### 🎨 Сучасний інтерфейс

- **Адаптивний дизайн** — працює на всіх пристроях
- **Анімації** — плавні переходи та CSS-анімації
- **Золота тема** — елегантний дизайн із золотими акцентами
- **Інтерактивні елементи** — прогрес-бари, календарі, модальні вікна

---

## 🏗️ Структура проєкту

```
TaskForge/
├── manage.py                         # Django CLI для управління проєктом
├── Procfile                          # Команди для деплою на Railway
├── requirements.txt                  # Python-залежності
├── runtime.txt                       # Версія Python (3.11.6)
│
├── main/                             # Головний Django-застосунок
│   ├── __init__.py
│   ├── models.py                     # Моделі даних (User, Habit, Goal, Notification, TelegramProfile)
│   ├── views.py                      # API endpoints та views (42+ функції)
│   ├── urls.py                       # URL-маршрутизація
│   ├── admin.py                      # Налаштування адмін-панелі Django
│   ├── apps.py                       # Конфігурація Django-застосунку
│   │
│   ├── tasks.py                      # Celery-задачі (сповіщення, нагадування)
│   ├── telegram_bot.py               # Telegram-бот (bind, 2FA, сповіщення)
│   ├── consumers.py                  # WebSocket consumers для real-time комунікації
│   ├── notification.py               # Система персоналізованих сповіщень
│   ├── activity_tracker.py           # Трекінг активності користувачів
│   ├── context_processors.py         # Контекстні процесори для шаблонів
│   ├── routing.py                    # WebSocket маршрутизація
│   │
│   ├── management/                   # Django management commands
│   │   ├── __init__.py
│   │   └── commands/
│   │       ├── __init__.py
│   │       └── create_tech_admin.py  # Команда створення технічного адміністратора
│   │
│   ├── migrations/                   # Міграції бази даних
│   │   ├── __init__.py
│   │   ├── 0001_initial.py
│   │   └── ...                       # Інші міграції
│   │
│   ├── static/                       # Статичні файли
│   │   ├── css/                      # Модульні стилі
│   │   │   ├── main.css              # Головний файл стилів
│   │   │   ├── base/                 # Базові стилі (змінні, reset, typography)
│   │   │   ├── components/           # Компоненти UI (buttons, forms, cards)
│   │   │   ├── layout/               # Структура сторінок (header, footer, grid)
│   │   │   └── pages/                # Стилі для окремих сторінок
│   │   │
│   │   ├── js/                       # Модульний JavaScript
│   │   │   ├── main.js               # Головний файл JavaScript
│   │   │   ├── notification.js       # Система сповіщень
│   │   │   ├── websocket-notifications.js  # WebSocket клієнт
│   │   │   ├── components/           # JavaScript-компоненти
│   │   │   ├── pages/                # Логіка для окремих сторінок
│   │   │   ├── utils/                # Допоміжні функції та утиліти
│   │   │   └── tests/                # Тести для JavaScript
│   │   │
│   │   └── img/                      # Зображення та іконки
│   │
│   ├── templates/                    # HTML-шаблони
│   │   ├── base.html                 # Базовий шаблон
│   │   ├── admin/                    # Шаблони адмін-панелі
│   │   │   ├── goaltemplate_form.html
│   │   │   └── habittemplate_form.html
│   │   └── pages/                    # Шаблони сторінок
│   │       ├── index.html            # Головна сторінка
│   │       ├── goals.html            # Сторінка цілей
│   │       ├── habits.html           # Сторінка звичок
│   │       └── statistics.html       # Сторінка статистики
│   │
│   └── tests/                        # Тести Django-застосунку
│       ├── __init__.py
│       ├── test_models.py            # Тести моделей
│       ├── test_views.py             # Тести views
│       └── test_tasks.py             # Тести Celery-задач
│
├── TaskForge/                        # Конфігурація Django-проєкту
│   ├── __init__.py
│   ├── settings.py                   # Налаштування Django (PostgreSQL, Redis, Channels)
│   ├── urls.py                       # Головна URL-маршрутизація
│   ├── asgi.py                       # ASGI-конфігурація для WebSocket
│   ├── wsgi.py                       # WSGI-конфігурація для production
│   ├── celery.py                     # Конфігурація Celery
│   └── routing.py                    # WebSocket routing
│
└── staticfiles/                      # Зібрані статичні файли (collectstatic)
    ├── admin/                        # Статика адмін-панелі Django
    ├── css/                          # Зібрані CSS-файли
    ├── js/                           # Зібрані JavaScript-файли
    └── img/                          # Зібрані зображення
```

---

## 🚀 Швидкий старт

### Вимоги

- **Python** 3.11+
- **PostgreSQL** 14+
- **Redis** 7+
- **Git**

### Встановлення

#### 1. Клонування репозиторію

```bash
git clone https://github.com/BrootBin/TaskForge.git
cd TaskForge
```

#### 2. Створення віртуального середовища

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# або
venv\Scripts\activate     # Windows
```

#### 3. Встановлення залежностей

```bash
pip install -r requirements.txt
```

#### 4. Налаштування змінних середовища

Створіть файл `.env` у корені проєкту:

```env
SECRET_KEY=your-secret-key-here
DEBUG=True
DATABASE_URL=postgresql://user:password@localhost:5432/taskforge
REDIS_URL=redis://localhost:6379/0
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
ALLOWED_HOSTS=localhost,127.0.0.1
```

#### 5. Міграції бази даних

```bash
python manage.py migrate
python manage.py createsuperuser
```

#### 6. Збірка статичних файлів

```bash
python manage.py collectstatic --noinput
```

### Запуск сервісів

У різних терміналах запустіть:

```bash
# Terminal 1: Django (Daphne)
daphne -b 0.0.0.0 -p 8000 TaskForge.asgi:application

# Terminal 2: Celery Worker
celery -A TaskForge worker --loglevel=info

# Terminal 3: Celery Beat
celery -A TaskForge beat --loglevel=info

# Terminal 4: Redis (якщо не запущений як сервіс)
redis-server
```

✅ Застосунок буде доступний за адресою: **http://localhost:8000**

---

## 🚂 Деплой на Railway

### Підготовка проєкту

#### 1. Створіть `Procfile` в корені проєкту:

```procfile
web: daphne -b 0.0.0.0 -p $PORT TaskForge.asgi:application
worker: celery -A TaskForge worker --loglevel=info
beat: celery -A TaskForge beat --loglevel=info
```

#### 2. Створіть `runtime.txt`:

```
python-3.11.6
```

#### 3. Переконайтеся, що `requirements.txt` актуальний:

```bash
pip freeze > requirements.txt
```

### Налаштування на Railway

#### 1. Створіть новий проєкт на [Railway.app](https://railway.app)

#### 2. Додайте PostgreSQL:

- **Add Service** → **Database** → **PostgreSQL**
- Railway автоматично створить `DATABASE_URL`

#### 3. Додайте Redis:

- **Add Service** → **Database** → **Redis**
- Railway автоматично створить `REDIS_URL`

#### 4. Деплой застосунку:

- **Add Service** → **GitHub Repo** → Оберіть **TaskForge**
- Railway автоматично визначить Python-проєкт

#### 5. Налаштуйте змінні середовища:

```env
SECRET_KEY=<generate-strong-secret-key>
DEBUG=False
ALLOWED_HOSTS=.railway.app
TELEGRAM_BOT_TOKEN=<your-bot-token>
DATABASE_URL=<auto-filled-by-railway>
REDIS_URL=<auto-filled-by-railway>
DJANGO_SETTINGS_MODULE=TaskForge.settings
```

#### 6. Налаштуйте сервіси:

- **Web Service**: Запускається автоматично з командою з Procfile
- **Worker Service**: Продублюйте сервіс і змініть команду запуску на `worker`
- **Beat Service**: Продублюйте сервіс і змініть команду запуску на `beat`

#### 7. Виконайте міграції (через Railway CLI або Railway Shell):

```bash
python manage.py migrate
python manage.py createsuperuser
python manage.py collectstatic --noinput
```

### Перевірка деплою

Після успішного деплою:

- ✅ Web-сервіс доступний за URL від Railway
- ✅ Celery worker обробляє задачі
- ✅ Celery beat надсилає періодичні сповіщення
- ✅ WebSocket-з'єднання працюють
- ✅ Telegram-бот відповідає на команди

### Моніторинг

Railway надає:

- 📊 Логи всіх сервісів у реальному часі
- 📈 Метрики використання ресурсів
- 🔄 Автоматичні деплої при push у `main`
- 🔧 Змінні середовища у web-інтерфейсі

---

## 📊 Основні моделі даних

### Goal (Ціль)

```python
class Goal(models.Model):
    title = CharField(max_length=200)           # Назва цілі
    description = TextField()                   # Опис
    target_date = DateField()                   # Цільова дата
    current_progress = IntegerField()           # Поточний прогрес
    target_value = IntegerField()               # Цільове значення
    is_completed = BooleanField()               # Статус виконання
    user = ForeignKey(User)                     # Власник
    created_at = DateTimeField()                # Дата створення
```

### Habit (Звичка)

```python
class Habit(models.Model):
    title = CharField(max_length=200)           # Назва звички
    description = TextField()                   # Опис
    frequency_type = CharField()                # Тип частоти (daily, weekly)
    target_count = IntegerField()               # Цільова кількість
    current_streak = IntegerField()             # Поточна серія
    user = ForeignKey(User)                     # Власник
    is_active = BooleanField()                  # Активність
    created_at = DateTimeField()                # Дата створення
```

### HabitCompletion (Виконання звички)

```python
class HabitCompletion(models.Model):
    habit = ForeignKey(Habit)                   # Пов'язана звичка
    date = DateField()                          # Дата виконання
    count = IntegerField()                      # Кількість виконань
    is_completed = BooleanField()               # Статус виконання
    created_at = DateTimeField()                # Час запису
```

---

## 🏗️ Технологічний стек

### Backend

- **Django 5.2.6** — сучасний web-framework
- **Django Channels 4.3.1** — WebSocket для real-time оновлень
- **Daphne 4.2.1** — ASGI-сервер для Django Channels
- **Celery 5.4+** — асинхронні задачі та планувальник
- **Redis** — message broker для Celery та кеш
- **PostgreSQL** — надійна реляційна база даних

### Frontend

- **Vanilla JavaScript** — модульна архітектура без фреймворків
- **CSS3** — сучасні стилі зі змінними та анімаціями
- **WebSocket API** — real-time комунікація із сервером

### Інтеграції

- **Telegram Bot API** — інтеграція з месенджером
- **Celery Beat** — періодичні задачі та нагадування
- **Django Channels** — WebSocket-layer для сповіщень

---

## 🎨 Дизайн-система

### Кольорова палітра

- **Primary**: `#D4AF37` (Золотий) — основні дії
- **Secondary**: `#2C3E50` (Темно-синій) — текст і контури
- **Success**: `#27AE60` (Зелений) — виконані задачі
- **Warning**: `#F39C12` (Помаранчевий) — попередження
- **Error**: `#E74C3C` (Червоний) — помилки

### Компоненти інтерфейсу

- Кругові індикатори прогресу
- Інтерактивний календар звичок
- Модальні вікна для створення/редагування
- Адаптивні картки цілей і звичок
- Система сповіщень та алертів

---

## 📱 Функціональність

### Дашборд

- Загальна статистика прогресу (денна та місячна)
- Швидкий доступ до активних звичок
- Календар виконання звичок
- Список актуальних цілей

### Керування звичками

- Створення звичок із налаштуванням частоти
- Щоденне відмічання виконання
- Відстеження серій (streaks)
- Статистика та тренди

### Керування цілями

- Створення цілей із дедлайнами
- Поділ на підцілі
- Відстеження прогресу
- Система досягнень

### Telegram-бот

- Щоденні нагадування
- Швидке відмічання звичок
- Отримання статистики
- Мотиваційні повідомлення

---

## 🔐 Безпека

- CSRF-захист для всіх форм
- Аутентифікація та авторизація користувачів
- Валідація даних на backend і frontend
- Безпечне зберігання Telegram-токенів
- HTTPS у production-середовищі

---

## 📈 Продуктивність

### Оптимізація завантаження

- Кешування даних звичок у `localStorage`
- Попереднє завантаження даних для критичних компонентів
- Оптимізовані SQL-запити з `select_related`
- Асинхронне завантаження статистики

### Моніторинг

- Логування критичних операцій
- Відстеження продуктивності database
- Моніторинг виконання Celery-задач

---

## 🧪 Тестування

```bash
# Запуск тестів Django
python manage.py test

# Запуск тестів із покриттям
coverage run --source='.' manage.py test
coverage report
coverage html

# Тестування frontend
cd frontend
npm test
```

---

## 📚 Документація для розробників

- **JavaScript Architecture** — детальна документація JS-архітектури
- **CSS Architecture** — документація дизайн-системи та CSS
- **API Documentation** — REST API endpoints
- **Deployment Guide** — керівництво з розгортання

---

## 🤝 Внесок у проєкт

1. Fork репозиторію
2. Створіть feature branch (`git checkout -b feature/amazing-feature`)
3. Commit змін (`git commit -m 'Add amazing feature'`)
4. Push у branch (`git push origin feature/amazing-feature`)
5. Відкрийте Pull Request

---

## 📄 Ліцензія

Цей проєкт поширюється під ліцензією MIT. Деталі — у файлі `LICENSE`.

---

## 👤 Автор

**BrootBin**

- GitHub: [@BrootBin](https://github.com/BrootBin)
- Версія: **2.0.0**
- Останнє оновлення: **листопад 2025 р.**

---

## 🔗 Корисні посилання

- [GitHub Repository](https://github.com/BrootBin/TaskForge)
- [Issue Tracker](https://github.com/BrootBin/TaskForge/issues)
- [Railway Deployment](https://railway.app)

---

## 📝 Changelog

### v2.0.0 (листопад 2025)

- ✨ Додано real-time систему сповіщень через WebSocket
- ✨ Реалізовано time-based polling (21:00–00:01)
- ✨ Персоналізовані сповіщення з іменами користувачів
- ✨ Візуальні індикатори непрочитаних сповіщень
- ✨ Анімовані золоті рамки для форм
- ✨ Підтримка 2FA через Telegram
- ✨ Технічна підтримка через модальне вікно
- 🐛 Виправлено помилки API для неавторизованих користувачів
- 🐛 Виправлено початок відмалювання progress circles (з 12 години)
- 🔧 Оптимізовано структуру коду (видалено 24 рядки дубльованого коду)
- 🔧 Покращено модульність JavaScript і CSS
- 📚 Оновлено документацію для деплою на Railway
